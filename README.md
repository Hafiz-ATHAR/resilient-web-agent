# resilient-web-agent
> A long-running background agent built with LangGraph and FastAPI that fetches and summarizes Wikipedia URLs with durable execution, SQLite checkpointing for crash recovery, real-time SSE progress streaming, and MLflow tracing.
